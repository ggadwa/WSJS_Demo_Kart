import ProjectClass from '../../../code/main/project.js';

export default class DemoClass extends ProjectClass
{
    mapCube(name)
    {
        return(null);
    }

    mapEffect(name)
    {
        return(null);
    }

    mapEntity(name)
    {
        return(null);
    }
}
